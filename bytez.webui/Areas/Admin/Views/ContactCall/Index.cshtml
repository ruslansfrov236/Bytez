@model List<ContactCall>


<div class="col-lg-12 grid-margin stretch-card">
    <div class="card">
        <div class="card-body">
            <h4 class="card-title">Create contactCall</h4>
            <a class="card-description" style="display:block;  margin:20px 0" href="/admin/contactCall/create">
                Add contactCall
            </a>
            <table class="table table-dark">
                <thead>
                    <tr>

                        <th> Name </th>
                        <th> Count </th>
                        <th> Create Date </th>
                        <th> Update Date </th>
                     
                        <th> Update  </th>

                        <th> Delete  </th>
                    </tr>
                </thead>
                <tbody>
                    @{
                        foreach (var contactCall in Model)
                        {
                            <tr>


                                <td> @contactCall.Phone  </td>
                                <td>  @contactCall.Description </td>
                                <td> @contactCall.CreatedDate</td>
                                <td> @contactCall.UpdatedDate</td>

                                <td>
                                    <a href="/admin/contactCall/update/@contactCall.Id">
                                        Update
                                    </a>
                                </td>

                                <td>
                                    <form method="post" asp-controller="contactCall" asp-action="Delete" asp-area="Admin" asp-route-id="@contactCall.Id">

                                        <button type="submit" class="btn btn-link"> Delete</button>
                                    </form>
                                </td>

                            </tr>
                        }
                    }


                </tbody>
            </table>
        </div>
    </div>
</div>